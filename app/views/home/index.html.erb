<% title "Welcome #{current_user.username}" %>

<% @flit = Flit.new %>

<%= form_for( @flit ) do |f| %>

    <textarea cols="40" id="flit_message" name="message" rows="20"></textarea>
    <%= submit_tag("Post") %>
<% end %>